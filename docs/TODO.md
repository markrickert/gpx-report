# TODO

Tracks work that is planned/wanted but not yet implemented, plus gaps found when auditing the other `docs/*.md` files against the actual code (2026-08-06). Keep this updated: per the rule in `CLAUDE.md`, any time the user asks for something to be built/changed, add it here first (even if it's about to be implemented in the same session) and check it off (with a short note, not a deletion) once it ships.

## Done

- [x] **PWA installability** (2026-08-06) — `frontend/vite.config.js` adds `vite-plugin-pwa` (`generateSW` strategy, `registerType: "autoUpdate"`), which generates `manifest.webmanifest` and a Workbox service worker at build time and auto-injects the manifest `<link>`/SW-registration script into `frontend/index.html` (a manual `apple-touch-icon` link and `theme-color` meta tag were added by hand alongside the existing title/theme-detection script, since the plugin doesn't inject those). Manifest `name`/`short_name` is "GPX Report", `theme_color`/`background_color` reuse the light-theme `--accent`/`--bg` values from `styles.css` (`#2563eb`/`#f9fafb`), `display: "standalone"`. Icons: no existing logo/icon asset anywhere in the repo, so four PNGs (`frontend/public/pwa-192x192.png`, `pwa-512x512.png`, `maskable-icon-512x512.png`, `apple-touch-icon.png`) were generated from scratch with a small pure-Node script (no ImageMagick/sharp available, wrote raw PNG bytes via `zlib.deflateSync` directly) — a rounded accent-blue square with a white polyline mark echoing the app's own elevation-chart look, plus a full-bleed variant with safe-zone padding for the maskable icon. Scope deliberately stops at "installable app shell, loads its own JS/CSS/HTML/icons offline": `generateSW`'s default precache list only covers same-origin build output, no `runtimeCaching` rules were added, and the GraphQL API lives on a separate origin (`VITE_GRAPHQL_URL`) the service worker never touches — offline/cached GraphQL data was explicitly not attempted, that's a much larger feature than installability. Verified via a headless Playwright check against the rebuilt container (`docker compose up -d --build frontend`): manifest `<link>` present, exactly one `ServiceWorkerRegistration` with scope `/` and an active controller after first load, and no console errors on a normal page load. A real "Add to Home Screen" prompt can't be triggered/verified from this environment (phone-browser-chrome UI, not scriptable) — not claimed as tested.
- [x] **Edit activity title, written back to source GPX file** (2026-08-06) — added `updateActivityTitle(id, title)` GraphQL mutation (`backend/src/graphql/{typeDefs,resolvers}.js`), a new `backend/src/gpx/writer.js` (`updateGpxTitle`) that rewrites the `<trk><name>` element in the source `.gpx` file via string replacement (no XML DOM lib added), then re-runs `processFile()` so the DB row and GPX file stay in sync. Frontend: `ActivityDetail.jsx` has an inline Edit/Save/Cancel affordance on the title. Only the title is editable for now — other metadata fields weren't asked for.
- [x] **Browser-based file viewer/editor** (2026-08-06) — asked for a simple read-only docs viewer with git history first; built it (`/docs` page + `repoTree`/`repoFile`/`fileHistory`/`fileDiff` GraphQL resolvers), then superseded it in the same session with a `code-server` service (`docker-compose.yml`) once write access became the actual goal — full VS Code in the browser, bind-mounted read-write at the repo root, port 8443, `--auth none` (Tailscale-only, no public exposure). The custom viewer code was removed rather than kept alongside it. See `docs/SETUP.md` §7.
- [x] **Code-server exposed as a tab in the frontend** (2026-08-06) — added a "Code" nav link / `/code` route (`frontend/src/pages/CodeEditor.jsx`) that iframes the existing code-server container at `http://<current-hostname>:8443`, derived at runtime from `window.location.hostname` (no build-time config, matching whatever host the app itself was loaded from). No new Caddy site or exposure — same LAN/Tailscale-only reach code-server already had, just one click away from the dashboard instead of a separate bookmark/port.
- [x] **Code tab switched to https via a Caddy site** (2026-08-06) — the runtime-derived `http://<hostname>:8443` iframe got blocked as mixed content once the dashboard was loaded over the public https domain. Added a `gpx-report-code.example.com` Caddy site (`reverse_proxy localhost:8443`, same pattern as the GraphQL API site) and a `VITE_CODE_SERVER_URL` build arg (baked in at image build time, matching `VITE_GRAPHQL_URL`) so the iframe loads over https. Still `--auth none` and Tailscale-only reach — the domain doesn't resolve/route outside Tailscale on this deployment, so the trust boundary is unchanged, just no longer mixed-content-blocked. See `docs/SETUP.md` §7. Also fixed the Code tab and dashboard content being narrower than the viewport (a stray `margin: 0 auto` and an overly tight 960px cap in `frontend/src/styles.css`).

- [x] **Dashboard activity list route thumbnail** (2026-08-06) — each row in `frontend/src/pages/Dashboard.jsx` now renders a small SVG polyline of the route shape next to the title/meta, built client-side from `activity.route.coordinates` (normalized to a 48x48 box, no basemap). Not a real Leaflet map: rendering 50 live tile-based maps per dashboard load would hammer OpenStreetMap's tile servers and be slow; a plain SVG sparkline needs no tiles and stays cheap at this scale. `GET_DASHBOARD` now also fetches `route { coordinates }` per activity.
- [x] **Dashboard infinite scroll** (2026-08-06) — `Dashboard.jsx` fetches `PAGE_SIZE=50` activities at a time and loads more automatically via an `IntersectionObserver` on a sentinel element at the bottom of the list, appending pages to local state as the user scrolls (rather than a fixed 50-activity cap with no way to reach older ones). Resets to page one when the activity-type filter changes since `useQuery`'s variables change. `GET_DASHBOARD` now takes `$limit`/`$offset`.
- [x] **Tighter Y-axis range on the elevation profile** (2026-08-06) — `ActivityDetail.jsx`'s elevation `LineChart` `YAxis` now uses an explicit `domain` of `[min elevation - 10, max elevation + 10]` (computed from `elevationData`) instead of the default 0-based domain, so variation is visible even when the route stays well above sea level. Falls back to `[0, "auto"]` if the profile is empty.
- [x] **Entrance animation for dashboard list items on scroll** (2026-08-06) — `Dashboard.jsx` rows start `opacity: 0`/translated down 12px and fade+slide to normal via a shared `IntersectionObserver` (a callback ref on each `<li>` observes it once on mount, adds its id to `visibleIds` state and unobserves on first intersection — matches the existing sentinel-observer pattern already used for infinite scroll). Applies to newly loaded pages too since new `<li>`s get their own ref call. Respects `prefers-reduced-motion` (`frontend/src/styles.css`).
- [x] **km/miles unit toggle** (2026-08-06) — new `frontend/src/units.jsx` holds a `UnitsProvider`/`useUnits()` React context (localStorage-persisted, key `gpx-report-units`, default `"imperial"`) plus shared `formatDistance`/`formatElevation`/`formatSpeed`/`distanceValue`/`elevationValue`/unit-label helpers. Wrapped around `<App>` in `main.jsx`. A `mi/ft` ↔ `km/m` toggle button lives in the nav (`App.jsx`, `.units-toggle` in `styles.css`). `Dashboard.jsx` and `ActivityDetail.jsx` (including the elevation chart's axis values/labels/domain padding) now read from this shared unit instead of hardcoding metric. `.js` extension had to become `.jsx` since the module needs JSX for the context provider.

## Planned features

- [ ] **`.slpz`/Slopes-export file support.** User exports ski activities from the Slopes app in `.slpz`-family format (files land in `data/gpx/` or a sibling dir, per user, though none present in the working tree yet) — need to ingest/log these alongside `.gpx` and view them the same way, to get full skiing-specific data ("that skiing goodness") the GPX pipeline doesn't capture. Format details, how it differs from GPX (likely has run-by-run/lift data, speed, vertical, etc.), and whether `gpxparser` can handle it or a new parser is needed all still need research before implementation.
 [ ] **Graceful degradation without JavaScript.** The frontend is a pure client-rendered Vite/React SPA — `index.html` is just an empty `<div id="root">` plus a script tag, so with JS disabled the app currently renders nothing at all. Making this degrade gracefully (rather than a blank page) is a real architectural shift, not a small tweak: likely needs server-side rendering or static pre-rendering of at least the Dashboard/ActivityDetail routes (a `<noscript>` message alone would just explain the blank page, not fix it). Scope/approach still to be worked out — worth deciding how much of the app (read-only browsing vs. the editing/trim features that need client-side interactivity anyway) is realistic to support this way before committing to an SSR framework.
- [ ] **In-app GPS recording.** Once installable as a PWA (see above), let the phone record a track live via the browser Geolocation API and save it as a new activity, instead of only ever importing files from elsewhere (Organic Maps export, Syncthing, etc.). Depends on PWA installability landing first (background/foreground GPS behavior needs a real installed context, not just a browser tab). Scope/approach still to be worked out.

## Done

- [x] **Linting/formatting: ESLint + Prettier with pre-commit hooks** (2026-08-06) — `backend/eslint.config.js` and `frontend/eslint.config.js` are separate ESLint 9 flat configs (`eslint@^9.9.0`, `@eslint/js`, `globals`; frontend adds `eslint-plugin-react@^7.35.0` + `eslint-plugin-react-hooks@^5.1.0` for JSX/hooks rules — v4 of `eslint-plugin-react-hooks` doesn't support ESLint 9, had to bump past what the task description suggested). A single root `.prettierrc.json`/`.prettierignore` (`prettier@^3.3.3`, double quotes/semicolons/trailing commas/100-char width to match existing style) apply to both, with `eslint-config-prettier` in each ESLint config so the two don't fight. New root `package.json` (repo previously had none) hosts `husky@^9.1.5` + `lint-staged@^15.2.9` purely because git hooks must live at the repo root; `.husky/pre-commit` runs `npx lint-staged`, which runs each subproject's own local `eslint --fix` then `prettier --write` on staged `.js`/`.jsx` files (scoped by path glob to `backend/**` vs `frontend/**`, invoking each subproject's local ESLint binary so plugin resolution stays correct). Verified end-to-end with a real scratch commit (reverted after). Existing source wasn't reformatted/autofixed as part of this — `npm run lint` is clean (0 issues) in both subprojects as of this commit, but `prettier --check` flags 11 pre-existing files (4 backend, 7 frontend) that don't match the new style; left alone as a separate cleanup, not bundled into this task.
- [x] **Trim/crop handles fixed on mobile browsers** (2026-08-06) — root cause turned out to be a real bug, not just missing scroll-suppression CSS: `TrimHandleShape` (`ActivityDetail.jsx`) only read `cx`/`cy` off its props and returned a bare `<rect>`, so recharts' `onMouseDown`/`onTouchStart` handlers (passed through via `ReferenceDot`'s `filterProps`) were silently dropped from the visible handle. Since that handle renders `isFront` on top of the invisible wide `ReferenceLine` catch-area, a touch landing on the handle itself (touch contact area is much larger than a mouse pointer, so this happens far more often on a phone) hit an element with no handler at all and the event never reached the line underneath. Fixed by spreading the rest of the props onto the `<rect>` (with explicit position/fill/stroke after the spread so they still win). Also added `touch-action: none` to `.elevation-chart` (`styles.css`) so a touch starting on the chart doesn't fight the page's default scroll/zoom gesture. Verified via Playwright with `hasTouch: true` mobile emulation and CDP-dispatched `touchstart`/`touchmove`/`touchend` events against the rebuilt frontend — both handles moved to the correct position, the trimmed-off head/tail shaded correctly, "Trim & Save" enabled, and the page didn't scroll during the drag. This is emulation, not a real phone — worth a quick check on an actual device if trim issues resurface, but the underlying bug (dropped event handlers) is confirmed and fixed independent of the emulation environment.
- [x] **`aggregatedStatsByType` query wired up** (2026-08-06) — new `/stats` nav tab (`frontend/src/pages/Stats.jsx`) renders a table of per-activity-type totals/averages (count, total/avg distance, total/avg duration, avg elevation gain) via a new `GET_STATS_BY_TYPE` query (`frontend/src/graphql/queries.js`). Uses the shared unit toggle (`units.jsx`) for distance/elevation. No date-range filter UI yet — the query supports `startDate`/`endDate` but this view always calls it unfiltered (all-time breakdown); a "this year" style filter wasn't asked for.
- [x] **Editable activity type, from a preselected list** (2026-08-06) — `ActivityDetail.jsx` gets an Edit/Save/Cancel affordance next to the activity-type badge (mirrors the existing title-edit flow), backed by a dropdown of the same 10 types Dashboard's filter uses (now shared via `frontend/src/activityTypes.js` instead of being defined twice, plus an "Unknown" option so activities without a guessable type can be left as-is). New `updateActivityType(id, activityType)` mutation (`backend/src/graphql/{typeDefs,resolvers}.js`) writes the `<trk><type>` element via a new `updateGpxType()` in `backend/src/gpx/writer.js` (same string-replace approach as the title writer), then re-runs `processFile()`. Since `gpx/parser.js`'s `resolveActivityType()` reads `<type>` back through a raw-value-to-label table (e.g. `mountain_biking` → "Mountain Biking"), the mutation converts the selected label back to its raw key first via a new `activityTypeToRawType()` export, so the round-trip lands on the exact label chosen rather than parser.js's generic camelCase/snake_case formatter guessing a slightly different string.
- [x] **Rounded elevation Y-axis tick labels on `ActivityDetail.jsx`** (2026-08-06) — the elevation `LineChart`'s `YAxis` gets a `tickFormatter={(value) => Math.round(value)}`, so ticks read as whole numbers regardless of unit (ft or m) instead of showing raw GPX decimal precision (e.g. `4117.4542`). The `domain` prop and the `label` (which already renders the correct unit via `elevationUnitLabel(unit)` from `units.jsx`) are unchanged.
- [x] **`.igc` file support (paragliding)** (2026-08-06) — new `backend/src/igc/parser.js` (`parseIgcFile`) parses IGC B-records (lat/lon/altitude/timestamp per fix) and `HFDTE` date headers via regex, computing distance/speed/elevation stats with a haversine helper (IGC has no precomputed distances the way `gpxparser` gives GPX), returning the same shape `gpx/parser.js`'s `parseGpxFile` does. `gpx/processor.js`'s `processFile()` now dispatches to whichever parser matches the file extension; `listGpxFiles`/the watcher (`gpx/watcher.js`) now match `.gpx` and `.igc`. IGC has no track-name/type header equivalent to GPX's `<trk><name>/<type>`, so title falls back to the filename stem and `activityType` is fixed to `"Paragliding"` (the only IGC source in use). Title/type editing (`updateActivityTitle`/`updateActivityType`) only works for `.gpx` files — `writer.js` only knows how to rewrite GPX XML — so both mutations now reject non-`.gpx` activities with a clear error, and `ActivityDetail.jsx` hides the Edit buttons for those activities (`gpxFilename` added to `GET_ACTIVITY`).

- [x] **Trim editing on the elevation profile** (2026-08-06) — no separate speed-vs-time chart added; the existing elevation `LineChart` on `ActivityDetail.jsx` already encodes speed (gradient line color) and rest stops (shaded bands), so trim editing landed on that one chart. Entering edit mode (existing Edit button, same `editMode` flag used by title/type editing) reveals two vertical drag handles directly on the chart (one per end of the kept range, rendered as a `ReferenceLine` + a circular `ReferenceDot` grab-knob per handle), draggable back and forth along the timeline via pointer/touch events routed through recharts' own `onMouseMove`/`onTouchMove` `activeTooltipIndex` (pixel-accurate against the chart's real scale, no manual pixel math). The chart shades whichever head/tail is currently cropped out via `ReferenceArea`, made more opaque than the kept region so it reads as "will be removed"; that shading (and the handles) stay until the trim is actually saved and the page's data is reloaded, at which point edit mode exits and the activity is back in plain data-viewing mode. The map polyline re-slices live as the handles move. New `trimActivity(id, startIndex, endIndex)` mutation (`backend/src/graphql/{typeDefs,resolvers}.js`) calls a new `trimGpxTrack()` in `backend/src/gpx/writer.js` (drops `<trkpt>` elements outside the kept index range via the same string-replace approach as the title/type writers, matching indices 1:1 against `parser.js`'s point order) then re-runs `processFile()`. Saving shows a `window.confirm` first, since it permanently deletes GPX track points with no undo; only `.gpx` activities get the trim UI, matching the existing title/type edit restriction.
  - **Recharts gotcha found and fixed while building this** (also silently affected the pre-existing rest-stop shading): the elevation chart's `XAxis` is keyed on `dist` (distance rounded to 2 decimals for display), which collides constantly on real tracks — a 240-point activity had only 154 unique `dist` values. Recharts' `ReferenceLine`/`ReferenceArea`/`ReferenceDot` silently render nothing at all (no error, no DOM node) when their `x`/`x1`/`x2` is matched against a category axis with duplicate values — confirmed via an isolated repro outside the app. Fix: added a second, hidden `XAxis` keyed on a synthetic always-unique `idx` field (`xAxisId="idx"`, `allowDuplicatedCategory={false}`) and pointed every `Reference*` component (rest-band shading included) at that axis via integer point indices instead of the `dist` string. Also: recharts only recognizes `Reference*` components as *direct* children of the chart — conditionally rendering a group of them wrapped in a `<>` Fragment silently drops all of them too, so each is its own top-level `{condition && (...)}` expression.

- [x] **Nicer-looking activity stats + overall visual design pass + dark mode** (2026-08-06) — done together since both touched the same styling surface. `frontend/src/styles.css` centers all colors on CSS custom properties (`--bg`, `--bg-elevated`, `--text`, `--text-muted`, `--border`, `--accent`, etc.) defined in `:root`, with a `:root[data-theme="dark"]` override block giving each variable a dark value. The `ActivityDetail.jsx` `metrics-row` (plain "Duration: 54m" text) became a `.metrics-grid` of `.metric-tile` cards (icon + value + label), matching the existing `.summary-tile` pattern Dashboard already used for its top stat row. Added active-nav-link styling (`App.jsx` switched `Link` → `NavLink` with an `isActive`-driven class) and gave cards/rows subtle hover states and border radii for general polish. The elevation chart's `Tooltip` got an explicit dark, semi-transparent `contentStyle` since recharts' default is a plain white box that would clash on a dark page.
  - **Manual light/dark toggle, since the initial `prefers-color-scheme`-only approach turned out not to be enough** (2026-08-06) — user needed a way to force light mode without changing the OS-level setting. New `frontend/src/theme.jsx` (`ThemeProvider`/`useTheme()`, same context+localStorage pattern as `units.jsx`, key `gpx-report-theme`) resolves the active theme as an explicit stored override if one exists, else the live `prefers-color-scheme` result (via a `matchMedia` change listener, so it still tracks the OS setting until the user overrides it once). Writes the resolved theme to `<html data-theme="...">`, which is what `styles.css`'s dark block now keys off instead of the media query directly. A sun/moon `.theme-toggle` button next to the existing units toggle (`App.jsx`) flips and persists the override. `frontend/index.html` has a small inline script that sets `data-theme` from `localStorage`/`matchMedia` before React mounts, so there's no flash of the wrong theme on load.
  - **Dark map tiles** (2026-08-06) — `ActivityDetail.jsx`'s Leaflet `TileLayer` was still the plain OSM light basemap regardless of theme, which stood out against a dark page. It now switches to CARTO's `dark_all` basemap (`https://{s}.basemaps.cartocdn.com/dark_all/...`) when `theme === "dark"`, OSM otherwise.
  - **Bigger, grey trim handles** (2026-08-06) — the elevation chart's drag handles were small blue circles pinned to the top of the chart. Replaced with a custom `TrimHandleShape` (a 14×36 grey rounded-rect pill rendered via `ReferenceDot`'s `shape` prop) vertically centered on the chart (`elevationMid`, the midpoint of `elevationDomain`) instead of pinned to the top — bigger drag target, and the color no longer competes with the blue speed-gradient line. The vertical guide lines through the trim range switched from blue (`#2563eb`) to the same grey (`#9ca3af`) to match.
- [x] **Hover-synced position dot on map + elevation profile** (2026-08-06) — `ActivityDetail.jsx` gets a single `hoverIndex` state shared by both views. On the elevation `LineChart`, `onMouseMove` reads recharts' `activeTooltipIndex` (same mechanism the trim handles already use) whenever a trim drag isn't in progress, and renders a small solid `ReferenceDot` (5px, blue fill/white stroke, targeting the existing hidden `xAxisId="idx"` axis) — deliberately understated next to the big grey trim-handle pills since it's not draggable. On the map, the `Polyline` gets `eventHandlers={{ mousemove, mouseout }}`; `mousemove` does a linear nearest-neighbor scan of `visiblePositions` against the event's `latlng` (fine at single-activity point counts) and sets the same `hoverIndex`, offset by the active trim range's start so indices line up with the untrimmed `elevationData`/`positions` arrays. A non-interactive Leaflet `CircleMarker` renders at `positions[hoverIndex]` whenever it falls inside the visible (post-trim) range. Both directions cleared on mouse-leave. Verified via Playwright: dispatched a `mousemove` over the chart and confirmed both the chart's `ReferenceDot` and a new marker `<path>` appeared on the map's SVG overlay at the corresponding route coordinate (screenshot-cropped around the marker's `getBoundingClientRect()` to confirm it sits on the polyline); separately dispatched a `mousemove` at a point on the rendered polyline and confirmed the chart's `ReferenceDot` appeared. Hover has no persistent equivalent on touchscreens — expected and out of scope per the ticket, this is a desktop-oriented interaction.

- [x] **Heatmap tab of all activity locations** (2026-08-06) — new `/heatmap` nav tab (`frontend/src/pages/Heatmap.jsx`) renders every activity's route as a single `leaflet.heat` density heatmap over the same theme-switched CARTO-dark/OSM-light tiles `ActivityDetail.jsx` uses, wired into `MapContainer` imperatively via `useMap()` + `L.heatLayer()` in a `useEffect` (the standard non-React-plugin pattern). Altitude representation: a "Color by elevation" checkbox switches to 4 separate heat layers, one per elevation quartile of the loaded dataset, each a single-color gradient with a swatch legend — chosen over a continuous per-point color scale since `leaflet.heat`'s `gradient` option is density-based, not value-based, so quartile banding was the simplest way to layer elevation onto it without a second charting library. Backend: new `heatmapPoints` query (`backend/src/graphql/{typeDefs,resolvers}.js`) returns `[lat, lon, elevation]` triples for every activity's route in one shot (not filtered/paginated — single-user scale), but samples each route down to a max of 300 points server-side rather than sending full GPS resolution: this deployment's 420 activities/~1.54M raw points would be ~24MB of JSONB and far more as JSON-over-the-wire, whereas sampled it's ~3.8MB, reasonable for a personal app. New `leaflet.heat@^0.2.0` frontend dependency.
- [x] **Stats page: activity heatmap-by-time-of-year** (2026-08-06) — `/stats` (`Stats.jsx`) now renders a hand-rolled GitHub-style calendar heatmap above the existing per-type table: a 7-row × ~53-column CSS grid of day squares for one year at a time, colored by activity count that day (5 intensity levels via `color-mix(in srgb, var(--accent) N%, var(--bg-subtle))`, so it tracks light/dark theme automatically). No new dependency — a small `react-calendar-heatmap`-style library would have needed roughly as much layout/styling code as the ~60-line hand-rolled grid, and this repo already prefers plain CSS classes over component libraries for single-view visuals. Frequency is the primary signal (square intensity = count); "type" is handled via a type dropdown filter (defaults to "All types") rather than color-coding each square by type, which got visually muddy at 12px-square scale — filtering to e.g. "Alpine Skiing" and switching years clearly shows the Nov–Dec clustering the ticket's "I only ski in winter" example describes. A year `<select>` (defaults to the most recent year with data) sits next to the type filter since this deployment spans 2022–2026. Data: new `GET_ACTIVITY_DATES` query (`frontend/src/graphql/queries.js`) calls the existing `activities` query with `limit: 1000` (no backend change — 420 activities fits in one round trip) fetching only `id`/`startTime`/`activityType`, bucketed into day/type counts client-side. On mobile, the grid's wrapper scrolls horizontally (`.calendar-heatmap-wrap`, same pattern as `.stats-table-wrap`) and cells shrink from 12px to 9px under the existing `@media (max-width: 600px)` block rather than squishing illegibly. Verified with Playwright screenshots (light, dark, 390px-wide mobile viewport) and by filtering to Alpine Skiing across years, confirming all 10 2024 ski activities render clustered in Nov/Dec with 2023 correctly showing an empty grid (no skiing that year).

## Known gaps

- [ ] **No DB migration tooling.** `backend/db/init.sql` only runs against a fresh volume. Any schema change to an already-deployed instance needs a manual `psql`/`ALTER` step. Fine for now (single-user, low change rate) but worth a lightweight migration runner if schema churn picks up.
- [ ] **No automated tests or typecheck config anywhere in the repo.** ESLint/Prettier now exist (see Done above); tests and typechecking are still unaddressed.
- [ ] **No auth on the API or frontend.** Acceptable for now since the intended deployment is Caddy + Tailscale (see `docs/SETUP.md` §6 reverse-proxy notes), but if the API/frontend domains are ever exposed outside Tailscale, this becomes a real gap, not just a v1 simplification.

## Explicitly out of scope for v1 (not gaps, just noting so they don't get re-litigated)

- No in-app GPX upload/file management — files are expected to arrive via Syncthing or manual drop into `data/gpx/`.
- No multi-user support.
